{
  "name": "elevator",
  "version": "1.0.0",
  "description": "Personal solutions to https://play.elevatorsaga.com/ game",
  "main": "solution/elevator.js",
  "types": "solution/elevator.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/typescript/bin/tsc --project ./",
    "postbuild": "npm run create_solution; npm run create_solution; npm run move_helper; npm run trim_solution; npm run copy_solution",
    "create_solution": "cp solution/elevator.js solution/elevator_solution",
    "move_helper": "perl -0777 -p -i -e '($helpers, $a, $b, $c) = m/((var __.*\\n( +.*\\n)+\\};\\n)+)/; ($init, $indent) = m/(solution = \\{\\n *init.*\\{\\n)( *)/; $helpers = join(\"\", split(/var __/, $helpers)); $helpers = join(\"\\n$indent\", split(/\\n/, $helpers)); s/(solution = \\{\\n *init.*\\{\\n)( *)/$1$2$helpers\\n$2/; print;' ./solution/elevator_solution",
    "trim_solution": "perl -0777 -p -i -e 's/(\\A(.|\\n)*solution = |(?<=\\n\\});(.*\\n){1,3}\\Z)//g' solution/elevator_solution",
    "copy_solution": "pbcopy < solution/elevator_solution"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mossman-drake/elevator.git"
  },
  "author": "mossman-drake",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mossman-drake/elevator/issues"
  },
  "homepage": "https://github.com/mossman-drake/elevator#readme",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.0.5"
  }
}
